# ~/.tmuxinator/ci-screen.yml

name: ci-screen
root: ~/xiv-repos/ci-screen

pre_window: workon ci-screen
windows:
  - editor:
      layout: main-vertical
      panes:
        - vim
  - tests: ls
  - devServer:
      layout: main-vertical
      panes:
          - vagrant status
